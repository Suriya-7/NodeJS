
// Multiple modules in the same file

// For additional
exports.add = (a,b) => a+b;

// For subtraction
exports.subtract = (a,b) => a-b;  

// For multiplication
exports.multiply = (a,b) => a*b;

// For division (with error handling for division by zero)
exports.divide = (a,b) => a/b;